#include "filter-luadump.h"

int filter_luadump ( struct HTTPBody *req, struct HTTPBody *res, void *p ) {
	//Just dump what was received to stdout or browser... 
	//Use an HTML table to get it right...
	return 0;
}
