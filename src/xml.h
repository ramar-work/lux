
#include <ztable.h>
#include <zhttp.h>
#include "util.h"

#ifndef XML_H
#define XML_H
char * xml_encode ( ztable_t *, const char * );
#if 0
char * xml_decode ( ztable_t *, const char * );
#endif
#endif
