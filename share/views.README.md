# Views

Any "view" related code goes here.  Typically, templates will go here.

Regular HTML can go here too if there is no need for any actual view code.  A route like:

```
	logicless = {
		view = "my-plain-html-page"
	},
```

would render a page without doing any templating.
