# private

Private files that should not be accessible to server users go here. (An example of what might go here is a list that is behind password protection.  The fs library can be used to dump the contents once a user has input a matching password.)
